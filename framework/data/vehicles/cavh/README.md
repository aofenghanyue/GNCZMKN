# CAVH Reference Vehicle Assets

This directory contains reference datasets for the built-in CAVH example
vehicle.

These files are assets, not runtime components.

Runtime components such as `aero.table2d` and `mass.constant` load these files
through their `asset_file` configuration and expose runtime capabilities from
`vehicle.output`.
