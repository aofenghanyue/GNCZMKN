# example_02_gravity_turn

这是独立程序式示例，不走 `user/active_project + gnc_sim` 这条工程装配链。

- 文件：`simple_gravity_turn.cpp`
- 用途：演示 `loadConfigString()` 和把仿真内核嵌入专用程序

如果你想做统一 mission 驱动的工程，优先参考：

- `user/example_01_minimal`
- `user/example_03_cavh_3dof`
